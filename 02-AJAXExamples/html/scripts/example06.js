window.addEventListener('load', () => {
	let el = document.querySelector("#anyName");
	const outTime = document.querySelector("#theTime");
	

	el.addEventListener('change', () => {
        //todo: Use fetch to get the time from the server and display it using a asynchronous function

    });
	
})
