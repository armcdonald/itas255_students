const circle = require('./modules/circle.js');

const myRadius = 4;

console.log(`The area of a circle of radius ${myRadius} is ${circle.area(myRadius)}`);

console.log(`The circumference of a circle of radius ${myRadius} is ${circle.circumference(myRadius)}`);
